platform :ios, '15.0'

target 'iCuno test' do
  use_frameworks!
  # или, если вдруг ругается:
  # use_frameworks! :linkage => :static

  pod 'YandexMapsMobile', '4.5.0-full'  # ← тут поставь СВОЮ версию из pod search

end
